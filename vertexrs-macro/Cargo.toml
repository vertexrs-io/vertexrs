[package]
name = "vertexrs-macro"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Procedural macros for the vertexrs incremental computation engine"
keywords = ["incremental", "dag", "pipeline", "proc-macro"]
categories = ["data-structures", "development-tools::procedural-macro-helpers"]

[lints]
workspace = true

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }
